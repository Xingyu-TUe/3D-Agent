# 地狱裂隙 · 本地开发路径

之后请在本机仓库开发。把下面的 `仓库根目录` 换成你电脑上的实际路径。

## 1. 拉取代码

```bash
git clone -b cursor/hell-rift-wechat-game-b0ee https://github.com/Xingyu-TUe/3D-Agent.git D:\RIFT
cd /d D:\RIFT
npm install
```

或直接双击 [`同步到本机.bat`](./同步到本机.bat)。机器可读路径表：[`local-paths.json`](./local-paths.json)（`root` 已为 `D:/RIFT`）。

---

## 2. 本机关键路径（相对仓库根）

| 用途 | 路径 |
|------|------|
| **仓库根目录** | `D:\RIFT`（本机固定） |
| H5 入口 | `index.html` |
| 源码 | `src/` |
| 共享启动 | `src/main.js` |
| 游戏主循环 | `src/Game.js` |
| 微信打包入口 | `src/wechat-entry.js` |
| 职业配置 | `src/Config/Character.json` |
| 技能 / 怪物数据 | `src/Data/skills.js` · `src/Data/enemies.js` |
| **素材源（改图改这里）** | `src/Assets/` |
| 角色 8 向动画 | `src/Assets/characters/druid/` · `hunter/` · `mage/` |
| 素材清单 | `src/Assets/manifestData.js` |
| 角色包清单 | `src/Assets/characters/packData.js` |
| 微信用素材副本 | `Assets/`（`build:wechat` 会从 `src/Assets` 同步） |
| 微信可导入目录 | `dist/wechat/` |
| 微信压缩包 | `dist/HellRift.zip` |
| 打包后的入口 | `game.js` |
| 脚本 | `scripts/` |

---

## 3. 日常命令

```bash
# 浏览器本地调试（推荐日常开发）
npm run dev
# 浏览器打开 http://localhost:8080
# 鼠标按住左半屏 = 摇杆

# 改完代码后打微信包
npm run build:wechat
# → 更新 game.js、dist/wechat/、dist/HellRift.zip、根目录 Assets/

# 重做素材
npm run generate:assets        # 怪/技能图标/UI/旧角色表
npm run generate:characters    # 8 向角色动画包
npm run generate:all           # 上面两个都跑

# 自测
npm run check
npm run test:sim
```

---

## 4. 微信开发者工具（本机）

**推荐**：导入

```text
<仓库根>/dist/wechat/
```

该目录内应直接看到：

- `game.js`
- `game.json`
- `project.config.json`
- `Assets/`（内含 `characters/` 等）

**不要**把「整个源码仓库」当长期微信工程目录（容易缺 `Assets/` 或扫到 `src/` 报错）。

改 `src/` 后：先 `npm run build:wechat`，再回开发者工具点「编译」。

---

## 5. 建议工作流

1. 本机用 Cursor / VS Code 打开仓库根目录  
2. `npm run dev` → 浏览器调玩法与 UI  
3. 需要真机/微信模拟器时再 `npm run build:wechat` → 打开 `dist/wechat/`  
4. 只改贴图：改 `src/Assets/...` → `npm run build:wechat`（或只复制到 `Assets/` / `dist/wechat/Assets/`）

---

## 6. 一键改本地根路径（可选）

编辑 `local-paths.json`：

```json
"root": "D:/RIFT"
```

其余 `paths` 都是相对路径，拼在 `root` 后面即为本机绝对路径。
