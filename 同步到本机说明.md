# 同步到本机 D:\3D-Agent

云端 Agent **无法直接写入** 你电脑上的 `D:\3D-Agent`。  
每次仓库更新后，请在本机运行一次同步脚本。

## 第一次（本机还没有代码）

1. 安装 [Git for Windows](https://git-scm.com/download/win) 与 Node.js  
2. 从仓库下载 [`同步到本机.bat`](./同步到本机.bat) 到任意位置（或先手动克隆）  
3. **双击** `同步到本机.bat`  
   - 会克隆分支 `cursor/hell-rift-wechat-game-b0ee` → `D:\3D-Agent`  
   - 执行 `npm install`、`npm run build:wechat`  
   - 自动打开资源管理器到该目录  

手动克隆等价命令：

```bat
git clone -b cursor/hell-rift-wechat-game-b0ee https://github.com/Xingyu-TUe/3D-Agent.git D:\3D-Agent
cd /d D:\3D-Agent
npm install
npm run build:wechat
```

## 之后每次更新

在 `D:\3D-Agent` 里双击根目录的 `同步到本机.bat`，或：

```bat
cd /d D:\3D-Agent
git pull origin cursor/hell-rift-wechat-game-b0ee
npm install
npm run build:wechat
```

## 路径约定

| 项 | 路径 |
|----|------|
| 本机仓库根 | `D:\3D-Agent` |
| 源码 | `D:\3D-Agent\src\` |
| 微信导入 | `D:\3D-Agent\dist\wechat\` |
| 路径配置 | `D:\3D-Agent\local-paths.json`（root 已设为 `D:/3D-Agent`） |
